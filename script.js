document.querySelector('.menu-toggle')?.addEventListener('click',()=>document.querySelector('.nav')?.classList.toggle('open'));document.getElementById('themeToggle')?.addEventListener('click',()=>document.body.classList.toggle('dark'));

const SHEET_CSV='https://docs.google.com/spreadsheets/d/1YCfoVNaRcGLA_n6Om9eXLoLmdvVQbh-T6Javx306ZTM/gviz/tq?tqx=out:csv';
function parseCSV(text){const rows=[];let row=[],cell='',quote=false;for(let i=0;i<text.length;i++){const c=text[i],n=text[i+1];if(c==='"'&&quote&&n==='"'){cell+='"';i++;}else if(c==='"'){quote=!quote;}else if(c===','&&!quote){row.push(cell);cell='';}else if((c==='\n'||c==='\r')&&!quote){if(c==='\r'&&n==='\n')i++;row.push(cell);if(row.some(x=>x.trim()))rows.push(row);row=[];cell='';}else cell+=c;}if(cell||row.length){row.push(cell);rows.push(row);}return rows;}
async function loadPublishedPosts(){const box=document.getElementById('sheet-posts');if(!box)return;try{const res=await fetch(SHEET_CSV);const rows=parseCSV(await res.text());if(rows.length<2){box.innerHTML='<p>এখনও কোনো লেখা জমা পড়েনি।</p>';return;}const headers=rows[0].map(x=>x.trim());const find=(names)=>{const i=headers.findIndex(h=>names.includes(h.trim()));return i<0?null:i};const titleI=find(['লেখার শিরোনাম','Title','শিরোনাম']);const authorI=find(['লেখকের নাম','Author','লেখক']);const catI=find(['লেখার বিভাগ','Category','বিভাগ']);const contentI=find(['আপনার লেখা এখানে লিখুন','Content','লেখা']);const statusI=find(['Status','status','প্রকাশনা']);const published=rows.slice(1).filter(r=>statusI!==null&&(r[statusI]||'').trim().toLowerCase()==='approved');if(!published.length){box.innerHTML='<p>এখনও কোনো লেখা প্রকাশের জন্য অনুমোদিত হয়নি।</p>';return;}box.innerHTML=published.reverse().map((r,i)=>{const title=r[titleI]||'শিরোনামহীন লেখা',author=r[authorI]||'পরিবারের সদস্য',cat=r[catI]||'লেখা',content=(r[contentI]||'').replace(/</g,'&lt;').replace(/>/g,'&gt;');return `<article class="article-row" style="display:block;padding:22px"><div><em>${cat}</em><h3>${title}</h3><small>লিখেছেন — ${author}</small><p>${content.slice(0,300)}${content.length>300?'…':''}</p></div></article>`;}).join('');}catch(e){box.innerHTML='<p>লেখা লোড করা যাচ্ছে না। Sheet-এর Publish to web সেটিংস পরীক্ষা করুন।</p>';}}
loadPublishedPosts();
